<%- include('common/header') %>
    <%- include('common/sidebar') %>
        <!-- User Table -->
        <div class="page-wrapper">
            <div class="page-content">
                <!-- Page Title & Breadcrumb -->
                <div class="row mb-3">
                    <div class="col-md-8">
                        <h3 class="fw-bold text-dark">
                            <%= title %>
                        </h3>
                    </div>
                    <div class="col-md-4 text-md-end text-muted">
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="#">Solar Installation and Maintenance Company</a></li>
                                <li class="breadcrumb-item active" aria-current="page">
                                    <%= title %>
                                </li>
                            </ol>
                        </nav>
                    </div>
                </div>
                <!-- Flash Messages -->
                <% if (success.length> 0) { %>
                    <div class="alert alert-success">
                        <%= success %>
                    </div>
                    <% } %>

                        <% if (error.length> 0) { %>
                            <div class="alert alert-danger">
                                <%= error %>
                            </div>
                            <% } %>
                                <div class="card shadow-sm border-0 rounded-4">
                                    <div class="card-body bg-light">
                                        <!-- Search and Controls -->
                                        <div class="row mb-3 align-items-center">
                                            <div class="col-md-6">
                                                <!-- Errors Search -->
                                                <input type="text" id="errorSearchInput" class="form-control"
                                                    placeholder="Search errors...">
                                            </div>
                                            <div class="col-md-6 text-end">
                                                <a href="add-error" class="btn btn-outline-secondary btn-sm grad-box">
                                                    <i class="bi bi-plus-circle"></i> Add Error
                                                </a>
                                            </div>
                                        </div>
                                        <div class="table-responsive">
                                            <table class="table table-hover align-middle" id="errorTable">
                                                <thead class="table auth-header-box">
                                                    <tr>
                                                        <th>Sr No</th>
                                                        <th>Error Code</th>
                                                        <th>Description</th>
                                                        <th>Action</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <% if (records && records.length> 0) { %>
                                                        <% records.forEach((row, index)=> { %>
                                                            <tr>
                                                                <td>
                                                                    <%= index + 1 %>
                                                                </td>
                                                                <td>
                                                                    <%= row.error_code %>
                                                                </td>
                                                                <td>
                                                                    <%= row.description %>
                                                                </td>
                                                                <td>
                                                                    <a href="/edit-error/<%= row.id %>"
                                                                        class="btn btn-sm btn-warning">Edit</a>
                                                                    <a href="/delete-error/<%= row.id %>"
                                                                        class="btn btn-sm btn-danger"
                                                                        onclick="return confirm('Are you sure you want to delete this error?')">Delete</a>
                                                                </td>
                                                            </tr>
                                                            <% }) %>
                                                                <% } else { %>
                                                                    <tr>
                                                                        <td colspan="5"
                                                                            class="text-center py-4 text-muted">
                                                                            <i
                                                                                class="bi bi-exclamation-circle fs-4"></i>
                                                                            <p class="mt-2 mb-0">No Errors Found</p>
                                                                        </td>
                                                                    </tr>
                                                                    <% } %>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
            </div>
        </div>
        <script src="/assets/js/script.js"></script>
        <%- include('common/footer') %>